# Home Assistant integration testing requirements

homeassistant==2026.9.2
pytest-homeassistant-custom-component==0.13.366
# Match HA core's openai_conversation pin for this HA version: the STT platform
# runs against core's SDK, and its constructor rules change between releases.
openai==2.45.0
# HA's tts component imports mutagen at module import; pin to core's tts manifest.
mutagen==1.48.1
# ...and HA's ffmpeg component (imported by tts); pin to core's ffmpeg manifest.
ha-ffmpeg==3.2.2
# The network audit's zwave adapter reads Z-Wave JS node and controller objects;
# its tests build the real ones. Pin to core's zwave_js manifest.
zwave-js-server-python==0.73.1
# The UPnP/IGD adapter reads the ssdp component's discovery cache through its
# public helper, and its tests hold a upnp config entry, so both core
# components must import. Pin to core's ssdp and upnp manifests.
async-upnp-client==0.48.1
getmac==0.9.5

pytest==9.*
pytest-asyncio==1.*

colorlog==6.12.0
PyTurboJPEG>=2.5.0
